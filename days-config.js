// IELTS MARATHON — which days are live right now.
// This is the ONLY file you need to edit when a new day goes live.
// index.html, teacher-dashboard.html, and checker-dashboard.html all
// read from this same list, so they can never drift out of sync.
const OPEN_DAYS = [1, 2, 3, 5, 6];
